# CO-154 Art: spell icons for the HUD slots

Ticket: [#239](https://github.com/danhquach/crimsononslaught/issues/239)

Each HUD slot (CO-142) showed a colour disc with one or two letters. This ticket gives all 20 roster spells an icon. The look is concept B of three: a round button shaded in the element's colour, with a pale silhouette of the spell on it. The disc colour says the element; the silhouette says the spell, so the five in an element must differ by shape alone.

One sheet per element, five discs in one row, in `SPELLS_BY_ELEMENT` order (`src/config/loadout.ts`). The cut frames are `icon.<spell id>.0` on their own atlas page 10, so no other page is re-quantised. The HUD draws the disc about 32 px across inside the 36 px slot, under the cooldown wedge; the silhouette stays in the middle 150/208 of the disc so the seconds badge on the rim never covers it.

Four sheets. Copy each whole `text` block below and paste it as the prompt, with the chosen concept picture attached as the reference. Nothing else to add.

If the tool has a negative-prompt field, paste this into it:

```text
text, letters, numbers, words, labels, captions, title, watermark, checkerboard, transparency grid, drop shadow, blur, smooth gradient, jpeg artifacts, glow halo outside the disc, square tiles, frames, panels
```

## 1. `icons_fire.png`: the five Fire spells

Save as `docs/art/sheets/CO-154/icons_fire.png`. Copy the whole block below; it is the complete prompt.

Delivered as: JPEG 1408x480 px from `openai/gpt-image-2` (asked 1280x256, 5x1 of 256 px), with concept B attached as the reference. The discs came back about 233 px across, about 270 px apart on 282 px cells, so each disc was found by its columns of art, not by grid. Everything outside each disc's circle, pulled in 1.5 px off the JPEG fringe, was set to #FF00FF, which also drops the faint noise the model left in the background. Each disc was then area-averaged onto a 36 px native cell with the disc 32 px across, averaging the art pixels alone and skipping any magenta-tinted speck inside the disc (the house rule, `b > 0.8r && g < 0.6r && r > 140`), and scaled back up 4x nearest-neighbour, so the cutter's 4x nearest-neighbour downscale lands on those averages. Saved as PNG (720x144, no metadata chunks) before the cut. Ruled grid band: no. `sheetCell` 144, `centred`; cuts to 36x36 native frames with a 32x32 art box.

```text
Create one sheet of five small round spell button icons for a top-down 2D pixel-art game, one icon for each of the five Fire spells, drawn side by side in one row. Draw only the five icons, no text, no player, no monsters and no ground.

The canvas is 1280 by 256 pixels, a grid of 256 pixel square cells, five cells across and one cell down. They read left to right. Each icon is a round disc 208 pixels across, centred in its cell, which leaves 24 pixels of plain magenta between the disc and every cell edge.

Every disc is the same, like the attached concept picture: a solid circle shaded in four flat rings of colour, a deep crimson rim, hex #6E0C0C, stepping inward through dark red #A3180F and orange-red #D9400F to a bright orange centre, hex #FF7A1A, with a crisp two pixel dark outline round the outside. The shading steps in hard bands with a little dithering between them, never a smooth blend. On each disc sits one bold, simple silhouette of the spell in pale warm cream, hex #FFF1D6, with a few bright yellow-white highlight pixels, hex #FFFBEA, outlined in a darker shade of the disc colour so it stands out. The silhouette is what tells the five apart: each one is a different shape, big and chunky, drawn with few details so it still reads when the icon is shown only 32 pixels across. Every silhouette stays inside the central circle of the disc, 150 pixels across, so a clear ring of plain disc colour shows all the way round it.

The first disc is Fire Bolt: a blazing fireball flying up and to the right: a round head with a short, tapering tongue of flame trailing behind it to the lower left. The second disc is Meteor: a big round rock plunging steeply down and to the left from the upper right, with a long straight streak of fire behind it and two small rock chips beside it. The third disc is Fire Wave: a low, wide wall of fire: three tall pointed flame tongues rising side by side from one flat base line, the middle one tallest, wider than it is tall. The fourth disc is Fire Companion: a small friendly kirin, a gentle deer-like beast, standing in side view facing right, with two short antlers and a little flame for a tail. The fifth disc is Fire Dragon: a horned dragon’s head in side profile facing right, jaws wide open, breathing a short burst of flame to the right.

Style, the same for every sheet in this project: chunky pixel art, crisp hard edges, no blur and no anti-aliasing; a grim fantasy look in the subject's own colours, never in the background; original design; match the attached reference picture.
Background: the file is one sheet of flat magenta #FF00FF with the discs sitting straight on it. Magenta runs unbroken from one edge of the canvas to the other, behind and between every disc alike, fully opaque, with no transparency or alpha channel and no texture, noise, shadow, glow or vignette.
The cells are a measurement, not something to draw: nothing whatever marks where one ends and the next begins — no tile, panel, square of colour, line, border, divider or frame — and the magenta beside a disc is the same magenta everywhere. No text anywhere either: a single character of text, a number, a label or a watermark ruins the sheet.
Every disc stays inside its own cell at the margin given above and never touches an edge; draw it smaller rather than let it spill, because the margin is measured on the delivered file. All five discs are exactly the same size.
```

## 2. `icons_ice.png`: the five Ice spells

Save as `docs/art/sheets/CO-154/icons_ice.png`. Copy the whole block below; it is the complete prompt.

Delivered as: Third try. The first two drew the companion as an ice serpent coiled into a shape that reads as the letter S, which the no-letters rule forbids; the block now asks for its head in profile. JPEG 1408x480 px from `openai/gpt-image-2` (asked 1280x256, 5x1 of 256 px), with concept B attached as the reference. The discs came back about 213 px across, about 270 px apart on 282 px cells, so each disc was found by its columns of art, not by grid. Everything outside each disc's circle, pulled in 1.5 px off the JPEG fringe, was set to #FF00FF, which also drops the faint noise the model left in the background. Each disc was then area-averaged onto a 36 px native cell with the disc 32 px across, averaging the art pixels alone and skipping any magenta-tinted speck inside the disc (the house rule, `b > 0.8r && g < 0.6r && r > 140`), and scaled back up 4x nearest-neighbour, so the cutter's 4x nearest-neighbour downscale lands on those averages. Saved as PNG (720x144, no metadata chunks) before the cut. Ruled grid band: no. `sheetCell` 144, `centred`; cuts to 36x36 native frames with a 32x32 art box.

```text
Create one sheet of five small round spell button icons for a top-down 2D pixel-art game, one icon for each of the five Ice spells, drawn side by side in one row. Draw only the five icons, no text, no player, no monsters and no ground.

The canvas is 1280 by 256 pixels, a grid of 256 pixel square cells, five cells across and one cell down. They read left to right. Each icon is a round disc 208 pixels across, centred in its cell, which leaves 24 pixels of plain magenta between the disc and every cell edge.

Every disc is the same, like the attached concept picture: a solid circle shaded in four flat rings of colour, a deep navy rim, hex #0A2463, stepping inward through dark blue #123C8C and blue #1F6FC4 to a bright sky-cyan centre, hex #4FC3F7, with a crisp two pixel dark outline round the outside. The shading steps in hard bands with a little dithering between them, never a smooth blend. On each disc sits one bold, simple silhouette of the spell in pale icy white, hex #EAF8FF, with a few bright pure white highlight pixels, outlined in a darker shade of the disc colour so it stands out. The silhouette is what tells the five apart: each one is a different shape, big and chunky, drawn with few details so it still reads when the icon is shown only 32 pixels across. Every silhouette stays inside the central circle of the disc, 150 pixels across, so a clear ring of plain disc colour shows all the way round it.

The first disc is Ice Arrow: a straight arrow made of ice pointing up and to the right, with a sharp faceted crystal head and frosty fletching. The second disc is Frost Nova Bomb: a round bomb with a short curled fuse on top, its body frosted over, with six short frost spikes bursting outward around it like a star. The third disc is Ice Shield: a kite-shaped shield of faceted ice crystal, pointed at the bottom, seen from the front. The fourth disc is Ice Companion: the head of a small friendly ice serpent in side profile facing right, a long rounded snout with a closed mouth and one round eye, a crest of three icicle spikes sweeping back from the top of its head, and a thick curved neck running down to the bottom left of the silhouette. The fifth disc is Ice Storm: a small puffy storm cloud across the top with slanted shards of ice rain falling from it down and to the left.

Style, the same for every sheet in this project: chunky pixel art, crisp hard edges, no blur and no anti-aliasing; a grim fantasy look in the subject's own colours, never in the background; original design; match the attached reference picture.
Background: the file is one sheet of flat magenta #FF00FF with the discs sitting straight on it. Magenta runs unbroken from one edge of the canvas to the other, behind and between every disc alike, fully opaque, with no transparency or alpha channel and no texture, noise, shadow, glow or vignette.
The cells are a measurement, not something to draw: nothing whatever marks where one ends and the next begins — no tile, panel, square of colour, line, border, divider or frame — and the magenta beside a disc is the same magenta everywhere. No text anywhere either: a single character of text, a number, a label or a watermark ruins the sheet.
Every disc stays inside its own cell at the margin given above and never touches an edge; draw it smaller rather than let it spill, because the margin is measured on the delivered file. All five discs are exactly the same size.
```

## 3. `icons_lightning.png`: the five Lightning spells

Save as `docs/art/sheets/CO-154/icons_lightning.png`. Copy the whole block below; it is the complete prompt.

Delivered as: Second sheet. The first came back on blue-violet discs as first specified; the element's colour in game is yellow (`fx_bolt`, #FFEE58), so the discs were recoloured by sending the block below with that first sheet attached and one line added in front: redraw it with yellow discs and keep all five silhouettes exactly as they are. JPEG 1408x480 px from `openai/gpt-image-2` (asked 1280x256, 5x1 of 256 px), with the first sheet attached as the reference. The discs came back about 239 px across, about 270 px apart on 282 px cells, so each disc was found by its columns of art, not by grid. Everything outside each disc's circle, pulled in 1.5 px off the JPEG fringe, was set to #FF00FF, which also drops the faint noise the model left in the background. Each disc was then area-averaged onto a 36 px native cell with the disc 32 px across, averaging the art pixels alone and skipping any magenta-tinted speck inside the disc (the house rule, `b > 0.8r && g < 0.6r && r > 140`), and scaled back up 4x nearest-neighbour, so the cutter's 4x nearest-neighbour downscale lands on those averages. Saved as PNG (720x144, no metadata chunks) before the cut. Ruled grid band: no. `sheetCell` 144, `centred`; cuts to 36x36 native frames with a 32x32 art box.

```text
Create one sheet of five small round spell button icons for a top-down 2D pixel-art game, one icon for each of the five Lightning spells, drawn side by side in one row. Draw only the five icons, no text, no player, no monsters and no ground.

The canvas is 1280 by 256 pixels, a grid of 256 pixel square cells, five cells across and one cell down. They read left to right. Each icon is a round disc 208 pixels across, centred in its cell, which leaves 24 pixels of plain magenta between the disc and every cell edge.

Every disc is the same, like the attached concept picture: a solid circle shaded in four flat rings of colour, a dark amber rim, hex #5A3A00, stepping inward through deep gold #9C6A00 and gold #E0B000 to a bright lemon yellow centre, hex #FFEE58, with a crisp two pixel dark outline round the outside. The shading steps in hard bands with a little dithering between them, never a smooth blend. The discs are a clean, bright electric yellow, never orange, never brown and never purple. On each disc sits one bold, simple silhouette of the spell in pure white with a few pale yellow highlight pixels, outlined all the way round in a thick dark amber-brown line, hex #3A2400, so it stands out clearly against the yellow. The silhouette is what tells the five apart: each one is a different shape, big and chunky, drawn with few details so it still reads when the icon is shown only 32 pixels across. Every silhouette stays inside the central circle of the disc, 150 pixels across, so a clear ring of plain disc colour shows all the way round it.

The first disc is Lightning Bolt: a crackling ball of lightning: a round glowing orb with short jagged arcs spiking out from it all the way round. The second disc is Chain Lightning: a jagged zigzag bolt leaping between three small round orbs in a chain, from the lower left up to the middle and down again to the lower right. The third disc is Tornado: a twisting funnel of wind, wide at the top and narrowing to a point at the bottom, with a small lightning spark beside it. The fourth disc is Lightning Companion: a small friendly thunderbird in flight facing right, wings spread wide, with a jagged lightning-shaped tail. The fifth disc is Lightning Sword: a straight sword pointing straight up with a crossguard and short grip, small jagged lightning arcs crackling along its blade.

Style, the same for every sheet in this project: chunky pixel art, crisp hard edges, no blur and no anti-aliasing; a grim fantasy look in the subject's own colours, never in the background; original design; match the attached reference picture.
Background: the file is one sheet of flat magenta #FF00FF with the discs sitting straight on it. Magenta runs unbroken from one edge of the canvas to the other, behind and between every disc alike, fully opaque, with no transparency or alpha channel and no texture, noise, shadow, glow or vignette.
The cells are a measurement, not something to draw: nothing whatever marks where one ends and the next begins — no tile, panel, square of colour, line, border, divider or frame — and the magenta beside a disc is the same magenta everywhere. No text anywhere either: a single character of text, a number, a label or a watermark ruins the sheet.
Every disc stays inside its own cell at the margin given above and never touches an edge; draw it smaller rather than let it spill, because the margin is measured on the delivered file. All five discs are exactly the same size.
```

## 4. `icons_earth.png`: the five Earth spells

Save as `docs/art/sheets/CO-154/icons_earth.png`. Copy the whole block below; it is the complete prompt.

Delivered as: JPEG 1408x480 px from `openai/gpt-image-2` (asked 1280x256, 5x1 of 256 px), with concept B attached as the reference. The discs came back about 245 px across, about 270 px apart on 282 px cells, so each disc was found by its columns of art, not by grid. Everything outside each disc's circle, pulled in 1.5 px off the JPEG fringe, was set to #FF00FF, which also drops the faint noise the model left in the background. Each disc was then area-averaged onto a 36 px native cell with the disc 32 px across, averaging the art pixels alone and skipping any magenta-tinted speck inside the disc (the house rule, `b > 0.8r && g < 0.6r && r > 140`), and scaled back up 4x nearest-neighbour, so the cutter's 4x nearest-neighbour downscale lands on those averages. Saved as PNG (720x144, no metadata chunks) before the cut. Ruled grid band: no. `sheetCell` 144, `centred`; cuts to 36x36 native frames with a 32x32 art box. Thin horizontal JPEG streaks crossing the first two discs average out below a visible tint at 32 px.

```text
Create one sheet of five small round spell button icons for a top-down 2D pixel-art game, one icon for each of the five Earth spells, drawn side by side in one row. Draw only the five icons, no text, no player, no monsters and no ground.

The canvas is 1280 by 256 pixels, a grid of 256 pixel square cells, five cells across and one cell down. They read left to right. Each icon is a round disc 208 pixels across, centred in its cell, which leaves 24 pixels of plain magenta between the disc and every cell edge.

Every disc is the same, like the attached concept picture: a solid circle shaded in four flat rings of colour, a dark umber rim, hex #3A230F, stepping inward through dark brown #5C3A1A and brown #8A5A2B to a warm ochre centre, hex #C8923F, with a crisp two pixel dark outline round the outside. The shading steps in hard bands with a little dithering between them, never a smooth blend. On each disc sits one bold, simple silhouette of the spell in pale sandstone cream, hex #F6EAD2, with a few bright white highlight pixels, outlined in a darker shade of the disc colour so it stands out. The silhouette is what tells the five apart: each one is a different shape, big and chunky, drawn with few details so it still reads when the icon is shown only 32 pixels across. Every silhouette stays inside the central circle of the disc, 150 pixels across, so a clear ring of plain disc colour shows all the way round it.

The first disc is Earth Spike: a sharp, jagged stone spike flying up and to the right, pointed at the tip and broken off rough at the back. The second disc is Boulder: a heavy round boulder whose surface is split by a web of thick cracks, the cracks glowing hot orange, hex #FF8A2A, like embers. The third disc is Earth Shield: a ring of five chunky stones circling an empty centre, evenly spaced round a circle. The fourth disc is Earthquake: a flat slab of ground split across the middle by one jagged fissure, with small chunks of rock jumping up out of the crack. The fifth disc is Earth Companion: a small friendly stone golem seen from the front, a boxy body of stacked rocks with thick arms and two round glowing eyes.

Style, the same for every sheet in this project: chunky pixel art, crisp hard edges, no blur and no anti-aliasing; a grim fantasy look in the subject's own colours, never in the background; original design; match the attached reference picture.
Background: the file is one sheet of flat magenta #FF00FF with the discs sitting straight on it. Magenta runs unbroken from one edge of the canvas to the other, behind and between every disc alike, fully opaque, with no transparency or alpha channel and no texture, noise, shadow, glow or vignette.
The cells are a measurement, not something to draw: nothing whatever marks where one ends and the next begins — no tile, panel, square of colour, line, border, divider or frame — and the magenta beside a disc is the same magenta everywhere. No text anywhere either: a single character of text, a number, a label or a watermark ruins the sheet.
Every disc stays inside its own cell at the margin given above and never touches an edge; draw it smaller rather than let it spill, because the margin is measured on the delivered file. All five discs are exactly the same size.
```
